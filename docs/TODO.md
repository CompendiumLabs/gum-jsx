# Todo

- `PngImage` / `LoadImage` and image upload
- vector PDF export (`@gum-jsx/pdf` has no next package yet)
- snapshot zooming and layout `depth` / `select` filters
- non-element REPL results in `exec`
